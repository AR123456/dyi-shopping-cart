export const cartReducer = (state, action) => {
  // setting up on action for which to run the switch case
  //   switch (action.type) {
  //     default:
  //       return state;
  //   }
  return state;
};
